function makePathAroundTheGrid(grid: Grid, padding: number): PathData {
  // this function should create path around grid with respect to its dimensions.
}

function DEBUG_pathVisualizer(renderables, scene) {
  // this function should draw line along the path
}
